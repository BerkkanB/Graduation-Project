export default function TeacherName({name}){
    return (
        <div className="mb-1 bg-dark d-flex text-white align-items-center justify-content-center" style={{height:"70px"}}>
            <h4>{name}</h4>
        </div>
    )
}